# Kamal's secrets for every destination (with -d, Kamal reads this and .kamal/secrets.<destination>, never
# .kamal/secrets), read on the machine that deploys. Only the registry token, from .kamal/kit.local.env: the admin
# login stays on the server (config/deploy.<destination>.yml, env-file).
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD
