// BetterChat client configuration. These values are public: they are the
// same Pusher app key / cluster that kick.com's own frontend ships to every
// browser (find them in DevTools on any kick.com page: Network -> WS).
window.BETTERCHAT_CONFIG = {
  pusherAppKey: '32cbd69e4b950bf97679',
  pusherCluster: 'us2',
  kickApiBase: 'https://kick.com/api/v2',
};
